part of octane.ds;

class OCTCDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Divider(
      height: 10,
      thickness: 1,
      color: OCTTColor.grey200,
    );
  }
}
